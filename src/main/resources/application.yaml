spring:
  datasource:
    url: jdbc:h2:file:./data/product_inv
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
  h2:
    console:
      path: /h2-console
      enabled: true
security:
  jwt:
    secret: JavaDoesnohoahffcanadsfafs2ffff
